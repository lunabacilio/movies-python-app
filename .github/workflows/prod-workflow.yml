name: Movies Prod Workflow
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: 
      group: movie-app
    steps:
      - uses: actions/checkout@v4
      - run: |
          ls 
          ls -la